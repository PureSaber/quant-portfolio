# quant-portfolio

Multi-strategy portfolio allocator for paper trading.

## Commands

```bash
pip install -e ".[dev]"
quant-portfolio status --config configs/example.yaml
pytest -q
ruff check src tests
```
